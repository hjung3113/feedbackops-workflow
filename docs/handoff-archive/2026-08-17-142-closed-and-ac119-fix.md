# Current continuation — #142 CLOSED (Phase A2b-part2 merged, PR #154); Phase B split into new issue #155; AC-119 flake fixed (PR #156) (2026-08-17, latest)

Full session history before this entry lives in `docs/handoff-archive/` (one file per
archived slice) — this file itself only ever holds the single most recent entry. Do not
append a new entry on top of this one; when a session ends, move THIS entry's content into a
new dated file under `docs/handoff-archive/` and replace this file with only the new entry.
Re-check `git status --short --branch`, `git log -5 --oneline --decorate`, `git fetch
--prune`, `gh issue list --state open --limit 100`, `git worktree list` before trusting
anything below — this entry is a snapshot as of `main` at `9be3e3c` (merge of PR #156).

## Addendum: the long-standing AC-119 flake is fixed (PR #156, merged)

Same session, after #142/#155 above. User asked to fix the one remaining
`run-all.sh` failure instead of leaving it as a known/accepted flake.
Root cause: `orchestrator-interface.smoke.sh`'s `AC-119-*` env-precedence
cases assert a total wall-clock window around `dispatch-core.sh`'s poll loop
(plus pre-marker delay where applicable) to prove which env var set the
timing — the upper bound only budgeted ~2s of headroom for `dispatch-core`'s
own pre-loop overhead (capability probe, receipt-writing node subprocesses,
retention-lock git calls) on top of nominal sleep time. That overhead now
regularly meets or exceeds the old budget (every case failed at exactly its
old boundary, reproduced identically on a clean checkout). Fixed by widening
only the upper bound per case (lower bound untouched — it's what actually
proves the interval/delay took effect). Verified clean standalone and in a
final `run-all.sh` (36/36), PR #156, CI green, merged (`9be3e3c`). This flake
had been carried as "known pre-existing, not fixed" across at least 4 prior
HANDOFF entries — it is no longer expected to appear in future `run-all.sh`
runs; treat a fresh AC-119 failure as new, not the same old flake.

## What happened this session

Continuing directly from the prior entry's "Phase A2b-part2 NOT started"
state. User asked to read HANDOFF, judge whether problems hit along the way
are toolkit-scope (file an issue) vs one-off, then implement Phase A2b-part2.

1. **Reused the live worktree** at
   `/Users/hyojung/orca/workspaces/feedbackops-workflow/issue-142-watchdog-progress-phaseA`,
   created a fresh branch `fix/issue-142-watchdog-progress-phaseA2b2` off
   current `origin/main` (the prior `...phaseA2b` branch was already merged),
   per the prior entry's own instruction to redispatch into the same
   worktree rather than create a new one.
2. **Read the full #142 issue** (both long comments) before touching code —
   confirmed the exact remaining scope: claude's known-good streaming-argv
   diff (write it back exactly as before), `transcribe_review()`'s NDJSON
   extraction step, the conductor-proposal handoff's same extraction, a
   claude-reviewer smoke case, and the doc update.
3. **Implemented Phase A2b-part2 directly** (no glm-5.3 dispatch — this
   session judged it as an extension of already-verified A1/A2a designs, not
   fresh unscoped work, so it stayed in CONDUCTOR's own hands):
   - `agent-runtime.sh`'s claude branch reads `progress-flags claude` from
     the registry instead of hardcoding `--output-format text`.
   - `transcribe_review()` gained an NDJSON-terminal-event-extraction step as
     the new first link in its fallback chain (NDJSON → whole-buffer JSON →
     fenced-regex), preserving every existing non-streaming fixture.
   - The `--conductor-control` proposal handoff extracts into a disposable
     temp file before calling `conductor-control-publish.sh`, which stays
     completely untouched (host-side security boundary, per the prior
     entry's explicit constraint).
   - New watchdog smoke coverage (`AC-142-A2b2-1`/`-2`) exercises claude's
     `--produce-review` path with real stream-json-shaped NDJSON fixtures.
   - `multi-agent-workflow.md`'s "Runtime-neutral stall watchdog" section
     documents the finished design.
   - PR #154 opened, `run-all.sh` 35/36 (the 1 failure,
     `orchestrator-interface.smoke.sh`'s AC-119/AC-130-5 cmux env-precedence
     cascade, independently reproduced on a **clean `main` checkout** before
     trusting it as pre-existing and unrelated — same flake documented in
     multiple prior HANDOFF entries).
4. **Opus scoped review of PR #154 returned FAIL** — 2 `block`, 4 `fix`, 3
   `nit` findings, all real:
   - **block**: the `--conductor-control` proposal path had no fenced-JSON
     fallback (only `transcribe_review()` did), so a real claude conductor
     proposal wrapped in prose would have been handed to
     `conductor-control-publish.sh`'s `JSON.parse` verbatim and refused —
     contradicting what the PR's own doc update claimed. Fixed by having the
     proposal handoff reuse `transcribe_review()` itself instead of calling
     `extract-final` directly.
   - **block**: zero smoke coverage for `--conductor-control` × claude
     NDJSON — the exact regression class that got A2b-part1 reverted. Added
     `AC-142-A2b2-4`/`-5` with a dedicated claude-NDJSON stub in
     `conductor-control.smoke.sh` (hit an issue-number collision with the
     existing 710/711/712 loop on first pass — fixed by moving to unused
     issue numbers 721/722).
   - **fix**: a non-string `final.text_path` value (`null`, a number, an
     object) threw an uncaught `TypeError` inside the node heredoc instead of
     falling back gracefully — added `typeof text === "string"` guards in
     both `transcribe_review()` and `runtime-registry.cjs`'s `extract-final`.
   - **fix**: the NDJSON-extraction gate read `PROGRESS.event_format ===
     "ndjson"` as proof a runtime is currently streaming — true for **all
     three** registered runtimes, so opencode/codex reviews would have
     silently entered the extraction branch the moment their output ever
     incidentally matched a final-event shape, contradicting the doc's own
     "a populated PROGRESS entry must never be read as proof of streaming"
     line. Fixed by adding a new explicit `PROGRESS.<runtime>.streams`
     boolean (`true` only for claude; `false` for codex/opencode) and gating
     on that instead.
   - **fix**: `agent-runtime.sh`'s claude branch discarded the registry
     read's exit status inside a process substitution — every other registry
     read in that file fails closed; this one didn't, so a broken registry
     would have silently reverted claude to no `--output-format` at all
     (i.e. silently reintroducing the #142 stall bug). Fixed to capture,
     check, and `machine_error` on failure.
   - **fix**: no test asserted the new argv actually reaches claude's
     process — the existing stub ignored `"$@"` entirely, so the new smoke
     cases would have passed even with the streaming-argv change reverted.
     Added `AC-142-A2b2-3` in `agent-runtime.smoke.sh`.
   - **3 nits**: hardcoded `"claude"` instead of `"$RUNTIME"` in the registry
     call (cosmetic — fixed), `extract-final`'s diagnostics being swallowed
     in the conductor block (moot after the block-1 fix, since that call
     site no longer calls `extract-final` directly), and the claude smoke
     stub's "partial" events not literally being `content_block_delta`
     frames (accepted as-is — the terminal-event and fenced-body shapes are
     what the test needed to prove, per the reviewer's own answer to Q5).
   - All fixes applied, all 3 named smoke suites reverified, `run-all.sh`
     rerun clean at 35/36 (same pre-existing flake, confirmed same
     signature). Pushed as a second commit on the same branch/PR.
5. **PR #154 merged** (CI green, `gh pr merge --merge`, repo convention) —
   **this auto-closed #142** via the commit's `Closes #142` trailer.
6. **Filed #155** for Phase B (opencode/codex progress-event streaming
   wiring) as a **separate new issue**, not a reopen of #142 — #142's
   literal bug (reviewer read-mode dispatches structurally guaranteed to
   stall) is genuinely fixed for claude, the only runtime with full
   empirical confirmation; Phase B is completeness/enhancement scope for the
   other two runtimes, which #142's own "Phase B" language in this
   HANDOFF's prior entries had already been informally tracking separately.
   #155 carries forward the exact blocking conditions from #142's research:
   codex blocked until the 2026-08-20 quota reset (schema confirmed via
   docs, incremental-timing behavior still unverified), opencode blocked on
   reproducing/ruling out upstream issue `#31435` (containerized-environment
   event-drop bug) through this repo's actual dispatch path first.

## Still open / not done this session

- **#155 (Phase B, opencode/codex streaming) is filed but undesigned** —
  same caveat as every previously-filed design issue in this history: needs
  its own design-then-review pass before dispatch, not ready-to-implement as
  filed.
- **#149 and #150 (filed last session) are still filed but undesigned** —
  unchanged, still need the same treatment.
- Every issue from 2 entries below (#129, #131, #132, #133, #134, #136,
  #137) remains open and untouched. **The old gating rule ("blocked behind
  #142 landing fully, Phase A AND B") is now StaleClaim + PartiallyTrue**:
  #142 itself is closed, so anything that was blocked specifically on #142's
  issue number is now technically unblocked — but re-check each one
  individually before assuming it's ready, since some may have really meant
  "blocked on the watchdog/streaming design being finished," which for
  opencode/codex specifically is still #155, not yet done.
- Worktree `/Users/hyojung/orca/workspaces/feedbackops-workflow/issue-142-watchdog-progress-phaseA`
  is now on a merged branch (`fix/issue-142-watchdog-progress-phaseA2b2`) —
  safe to reuse for #155 (create a fresh branch from current `main` in the
  same worktree) or remove, not decided this session.
- Scratch untracked dispatch artifacts left inside that worktree's `.review/`
  (`ISSUE-142-PARTIAL-UNTRACKED*`, `ISSUE-142-launch.gOQ55U/`) were never
  cleaned up — predate this session, left untouched, safe to remove if
  confirmed stale.

## Next session

1. `git fetch --prune` + `git pull`/rebase onto current `origin/main` FIRST —
   confirm `main` is past `9be3e3c` (PR #156 merged, AC-119 flake fix) before
   trusting anything above. The main worktree
   (`/Users/hyojung/Desktop/2026/feedbackops-workflow`) was left checked out
   on branch `fix/ac-119-timing-flake-widen-bounds` (already merged) at
   session end — `git checkout main` first. That branch and
   `fix/issue-142-watchdog-progress-phaseA2b2` are both safe to delete
   locally/remotely, already merged.
2. Re-triage the Tier 2/3 backlog (#129, #131, #132, #133, #134, #136, #137)
   now that #142 itself is closed — check each issue body's own stated
   blocking condition individually rather than assuming the old blanket
   "blocked behind #142" rule still applies verbatim.
3. #155 (Phase B) needs the same design-then-review pass #142 itself got
   before any dispatch: codex is hard-blocked until 2026-08-20 regardless;
   opencode needs the real-dispatch-path reproduction test for issue #31435
   before its design can be finalized.
4. Apply this session's process precedent: when a scoped PR's own Opus
   review returns FAIL, fix every block/fix finding, re-verify the same
   named smoke suites plus one more full `run-all.sh`, then push a second
   commit onto the same branch/PR rather than opening a new one — this kept
   the same #142-phase-review discipline every prior phase used.
5. Preserve the same protected untracked paths listed in every prior entry
   below (unchanged this session): `.agents/skills/meta-prompt`,
   `.claude/`, `.opencode/`, `.review/ISSUE-70-launch.*`,
   `.review/ISSUE-71-launch.*`, `.review/open-issues-dag-2026-08-02.md`,
   `.review/open-issues-dag-2026-08-14.md`, `.review/open-issues-dag-2026-08-15.md`,
   `HANDOFF.md`, `docs/research/2026-07-27-external-model-scorecard-sources.md`,
   `docs/research/2026-08-15-full-feature-catalog.md`,
   `docs/plans/2026-08-15-feature-abstraction-two-axis-design.md`,
   `.review/DESIGN-REVIEW-two-axis-PROMPT*.md`.
