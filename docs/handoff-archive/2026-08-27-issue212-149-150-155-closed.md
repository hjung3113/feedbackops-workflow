# Current state — #220, #217 closed; main clean, pushed (2026-08-26)

Prior entry archived to `docs/handoff-archive/2026-08-25-issue221-merged.md`.
Full history lives in `docs/handoff-archive/`. This file only ever holds the single most
recent entry — do not append; move THIS entry's content to a new dated archive file when
the session ends, then replace this file with only the new entry. Re-check `git status
--short --branch`, `git log -5 --oneline --decorate`, `gh issue list --state open --limit
50`, `gh pr list --state open`, `git worktree list` before trusting anything below — this
entry is a snapshot, not a plan.

## What happened this session

Worked the priority list from the prior handoff, top to bottom.

**#220** (omp doc/smoke follow-ups, deferred from PR #219) — closed. Investigated both
items before touching anything:
- Item 1 (stale-enumeration detector) was **already fixed** by #219 itself —
  `runtime-registry-containment.smoke.sh` already pins the full 4-runtime set
  (codex/claude/opencode/omp). Nothing to do.
- Item 2 (`toolkit/docs/agents/conductor-persona.md`) was genuinely stale — 3 spots
  (lines 3, 17, 67) still described runtime selection as Codex/Claude Code/OpenCode only,
  omitting omp. Fixed directly (trivial docs, no dispatch needed per this repo's own
  "trivial work: execute directly" convention). Committed `b0d3816`, pushed to main,
  closed #220 with a comment citing both findings.

**#217** (dispatch tooling should detect execution environment and pick matching adapter)
— closed. This one needed real design work first, so investigated rather than guessing:
- Confirmed `toolkit/scripts/adapters/herdr.sh` already had a reliable self-detection
  predicate (`session_context_available()`: `HERDR_ENV=1` + all three
  `HERDR_WORKSPACE_ID`/`HERDR_TAB_ID`/`HERDR_PANE_ID` non-empty).
- Confirmed orca and cmux have **no equivalent general ambient signal** —
  `ORCA_CLI_COMMAND` only fires for managed WSL sessions (per
  `~/.agents/skills/orca-cli/SKILL.md`), not a general "inside an orca pane" check; cmux
  has nothing comparable (`CMUX_LIVE_*` are adapter-internal outputs, not ambient env).
  This asymmetry is the actual shape of the design decision, not a symmetric 3-way check.
- Asked the user 3 clarifying questions (helper location, decision-rule strength,
  execute-now-vs-defer). Answers: shared lib function; **always** prefer herdr when
  detected unless user/task explicitly names another transport; dispatch to codex rather
  than hand-implement.
- User then said "herdr로 하자" (use herdr, not orca) — which is literally #217's own rule
  applied live: this session's own `HERDR_ENV=1`/workspace/tab/pane were all set, so herdr
  was the correct dispatch choice, confirmed by checking `env` before dispatching.
- Dispatched to `gpt-5.6-luna max` (Implementation tier) via **herdr** (`herdr worktree
  create --branch fix/issue217-transport-detection`, `herdr agent start fix217 --kind codex
  -- --model gpt-5.6-luna -c model_reasoning_effort="max"`, full task-brief file copied
  into the worktree, `herdr agent prompt fix217 "read ISSUE-217-PROMPT.md..." --wait
  --timeout 1800000`). The `--wait` call itself backgrounded past its harness timeout and
  returned via a task notification — checked `herdr agent get fix217` (status `done`)
  before trusting it, matching the documented pattern from prior sessions' herdr dispatch.
- Codex delivered exactly the scoped brief: `detect_current_transport()` +
  `session_context_available()` centralized in `toolkit/scripts/lib/adapter-helpers.sh`
  (herdr.sh now sources the shared predicate instead of defining its own), preference rule
  wired into `model-routing/SKILL.md` and `conductor-persona.md` line 10, 9 new smoke
  cases in `orchestrator-interface.smoke.sh` (all 4 HERDR_* vars × unset/empty). Did not
  touch `agent-workflow.sh`/`dispatch-core.sh` `--orchestrator` flag (correctly, per
  brief) — this stays a documentation/skill-guidance layer for the human/assistant caller,
  not a code-level auto-selection, matching the issue's own framing.
- Reviewed the actual diff myself (not just the agent's self-report): confirmed the shared
  predicate has exactly one definition, all 5 existing `herdr.sh` call sites still resolve
  it correctly via the sourced lib, both doc edits read cleanly. Re-ran the smoke suite
  myself: `ALL CASES PASS`. `bash -n` clean on all 3 changed shell files, `git diff
  --check` clean.
- Committed `725746d` on the worktree branch, fast-forward merged to `main`, pushed to
  `origin/main`. Closed #217 with a summary comment. Cleaned up: `herdr worktree remove
  --workspace wW --force`, then `git branch -d fix/issue217-transport-detection`.

Both fixes landed on `main` this session: `b0d3816` (#220), `725746d` (#217).

## Next session (superseded — see this file's own successor entry for what actually happened)

Per prior HANDOFF's priority order, remaining items (unchanged, this session did not touch
them):

- **#212** (herdr trust-prompt-blocked launch detection) — still open, still
  design-blocked, low urgency (codex's actual production risk is separately closed via
  #215/#218/#221's pre-seed + hardening, real-binary verified). Sole remaining blocker on
  herdr's own T5 eligibility.
- **#155** (codex/opencode progress-event streaming, phase B of #142).
- **#150** (design: adapter-axis abstraction should wire existing orca-cli primitives —
  e.g. orca terminal read/show as an auxiliary liveness signal, not a replacement for
  RUN.json polling). Explicitly kept separate from #217 this session — different scope,
  do not conflate.
- **#149** (design: tiered dispatch — trivial-tier bypass).

**Untouched, unrelated to this session:** #208 (cmux `cmux run` primitive doesn't exist on
real cmux 0.64.x — **ON HOLD**, do not pick up opportunistically, see prior archived
entries).

**Unresolved from prior sessions, still not investigated:** dispatching to a fresh `codex`
TUI terminal via `orca terminal create` hit codex's own "Update available" prompt on
launch; `orca terminal send` refused all input (`agent_prompt_blocked` /
`blockedReason: "codex-update-prompt"`). No documented orca-cli workaround found yet. Not
hit this session (herdr dispatch was used for #217, same as prior sessions — this failure
mode is specific to orca-terminal dispatch of a fresh codex TUI). Worth filing against orca
if it recurs.

**`generic-conductor-persona` worktree still has uncommitted, unmerged work**
(`/Users/hyojung/orca/workspaces/feedbackops-workflow/generic-conductor-persona`, branch
`fix/generic-conductor-persona`, `bd82991`) — flagged again, still not investigated (out of
scope, carried forward from prior sessions). Check with the user before touching it if it
comes up.
